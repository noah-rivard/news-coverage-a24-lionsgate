# Agent Trace Logs

This folder stores optional, manually enabled agent trace logs for debugging.

Notes:
- Traces are not written by default.
- Use the CLI flag `--trace` (or `--trace-path`) to enable a log for a run.
- Logs may include full article content and should not be committed.
